#!/usr/bin/env bash
# SERVER: Step 1.2 (dataset download + schema inspection).
# Usage (from repo root): bash scripts/server/01_download_data.sh
source "$(dirname "$0")/_common.sh"
activate_venv
start_log

step "01_download_data: not implemented yet — placeholder for Step 1.2 (dataset download + schema inspection)"
exit 1
