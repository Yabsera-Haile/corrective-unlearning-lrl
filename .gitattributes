# Server scripts must keep LF endings even when committed from Windows,
# otherwise bash on the server fails with "$'\r': command not found".
*.sh   text eol=lf
*.py   text eol=lf
*.yaml text eol=lf
*.md   text eol=lf
